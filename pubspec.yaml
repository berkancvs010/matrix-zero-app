name: matrix_zero
description: "Sıfır Log Sohbet ve Sesli Arama"
publish_to: 'none'
version: 1.0.8+12
environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  crypto: ^3.0.6
  web_socket_channel: ^3.0.3
  flutter_webrtc: ^1.5.2
  file_picker: ^12.0.0
  shared_preferences: ^2.5.5
  image_picker: ^1.1.2
  image: ^4.5.4
  path_provider: ^2.1.5
  proximity_sensor: ^1.4.0
  flutter_secure_storage: ^11.0.0
  firebase_core: ^4.13.0
  firebase_messaging: ^16.5.0
  flutter_local_notifications: ^22.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:

  uses-material-design: true

